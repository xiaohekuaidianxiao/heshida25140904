# 实验报告（待补充）

## 1. 实验概述

将 ReAct 架构用于逆向分析场景。

## 2. 目标程序分析

```text

main()
  │
  ├─ printf("Enter password: ")
  ├─ scanf("%9s", password)        ← 读入最多9字符，存入栈上10字节缓冲区
  ├─ check_password(password)
  │      │
  │      ├─① strlen(input) < 4 ?
  │      │    YES ──→ puts("Wrong password!") → return 0 ──→ main return 0
  │      │    NO  ──→ 继续
  │      │
  │      ├─② input[0] == 'A' ?
  │      │    NO  ──→ (跳出外层if) → puts("Wrong password!") → return 0
  │      │    YES ──→ 继续
  │      │
  │      ├─③ input[1] == 'B' ?
  │      │    YES ──→ gadget_trap()
  │      │    │         ├─ puts("Oops! You are trapped...")
  │      │    │         └─ while(1){}  ← 死循环，程序永不返回
  │      │    NO  ──→ 继续
  │      │
  │      ├─④ input[1] == 'Z' ?
  │      │    NO  ──→ (跳出内层if) → puts("Wrong password!") → return 0
  │      │    YES ──→ 继续
  │      │
  │      ├─⑤ (input[2] ^ 0x12) == 'q' ?
  │      │    NO  ──→ puts("Wrong password!") → return 0
  │      │    YES ──→ 继续
  │      │
  │      ├─⑥ (input[3] + 3) == 'H' ?
  │      │    NO  ──→ puts("Wrong password!") → return 0
  │      │    YES ──→ puts("Success! Flag is found.") → return 1
  │      │
  │      └─ (所有不满足的分支最终汇聚到末尾)
  │           puts("Wrong password!") → return 0
  │
  └─ return 0

```



## 3. 工具封装设计（Task 1）tools.py

### 4.1 controlled_explore

在指定的 find/avoid 约束下，驱动 SimulationManager 探索路径。

    参数：
        binary_path  : str        - 可执行文件路径
        find_addrs   : list[int]  - 目标地址列表（如 Success 输出的基本块地址）
        avoid_addrs  : list[int]  - 需避开的地址列表（如死循环相关地址）
        max_steps    : int        - explore 的最大步数上限
    
    返回：
        dict - 结构化观察结果

### 4.2 solve_input

- 功能描述
- 求解逻辑说明
- 返回值说明

## 5. ReAct 主循环设计（Task 2）

- 系统提示词设计思路
- LLM 输出解析策略
- Observation 构造方式

## 6. 运行日志

完整的 3 轮 Thought → Action → Observation 轨迹

## 7. 思考题

### 7.1 朴素符号执行为何容易卡死？

程序中 gadget_trap() 包含 while(1) 死循环。朴素符号执行调用
explore() 时若不指定 avoid 约束，angr 会对死循环路径持续展开，
每轮迭代都产生新的路径节点，导致路径数量指数增长（路径爆炸），
内存持续增长直至耗尽，且永远无法达到终止状态。

### 7.2 LLM 的角色与作用

LLM 在本实验中承担决策编排层的角色：
(1) 语义理解：理解程序的业务逻辑（AB→死循环，AZ→成功）
(2) 搜索裁剪：基于语义理解决定 avoid 死循环地址、find 成功地址
(3) 策略规划：通过 ReAct 循环逐步推理，先探索再求解
(4) 常识利用：利用对程序结构的判断，避免盲目搜索
